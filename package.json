{
  "name": "finance-dev",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo of finance + technology demo tools (portfolio shell + isolated tools).",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "nx run-many -t build",
    "lint": "nx run-many -t lint",
    "typecheck": "nx run-many -t typecheck"
  },
  "devDependencies": {
    "nx": "20.3.0",
    "typescript": "5.6.3"
  }
}
